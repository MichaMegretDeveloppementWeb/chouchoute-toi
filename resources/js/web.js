/* The falcon/analytics collector belongs here and not in admin.js: the
   administration's own visits are not measured. */
import '../../vendor/falcon/analytics/resources/js/collector.js';

import './app.js';

/* Rien du kit ni des paquets ici · chacun compile son script, le livre, et
   cette application en publie une copie. Le kit pose ses balises et celles des
   paquets dans les pages qu'il dessine, par `@falconScripts`.

   Les deux `import` qui vivaient ici pointaient vers `dist/`, que ni le kit ni
   falcon/booking ne portent plus. */

import './app.js';
import './admin/sidebar.js';
